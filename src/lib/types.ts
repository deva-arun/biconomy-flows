/**
 * Shared TypeScript types for the Biconomy Benchmark Suite
 */

// Session execution mode
export type SessionMode = 'ENABLE_AND_USE' | 'USE';

// Button variant types
export type ButtonVariant = 'primary' | 'success' | 'danger' | 'secondary';
